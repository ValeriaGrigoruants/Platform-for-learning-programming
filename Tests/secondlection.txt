But the elements in an array can't be explicitly initialized to specific values when it is declared.
	Wrong: can't
	Right: can

By default, regular arrays of local scope are left initialized.
	Wrong: initialized
	Right: uninitialized

